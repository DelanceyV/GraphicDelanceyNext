---
deployment:
  tasks:
    - export DEPLOYPATH=/home/grapxdwk/public_html/
    - /bin/cp -R components $DEPLOYPATH
    - /bin/cp -R pages $DEPLOYPATH
    - /bin/cp -R public $DEPLOYPATH
    - /bin/cp -R shared $DEPLOYPATH